#!/usr/bin/env bash

set -e

if [[ $# -lt 1 ]]; then
  echo "Usage: script-hook [name] [args...]"
  exit 1
fi

HOOK=$1
HOOK_PATH="$DOT_DATA_DIR/hooks/$HOOK"
shift

if [[ -f $HOOK_PATH ]]; then
  bash "$HOOK_PATH" "$@"
fi
