#!/usr/bin/env bash

case "${EDITOR:-nvim}" in
*nvim | *vim)
  exec launch_nvim_tmux.sh "$@"
  ;;
*nano | *micro | *hx | *helix)
  script-launch-terminal "$EDITOR" "$@"
  ;;
*)
  exec setsid uwsm-app -- "$EDITOR" "$@"
  ;;
esac
